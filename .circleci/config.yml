version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:latest
    steps:
      - run: echo "Build"
  deploy:
    docker:
      - image: circleci/python:latest
    steps:
      - run: echo "Deploy"

workflows:
  version: 2

  stage_workflow:
    job:
      - build:
          filters:
            branches:
              only: master
      - deploy:
          filters:
            branches:
              only: master
          requires:
            - build